{% extends 'base.html.twig' %}

        {% block content %}
        <div id="user-page">
            <div class="user-welcome">
                <h1>Bienvenue {{ user.username }}</h1>
                <p>Bienvenue sur votre parcours, retrouvez ici la liste des différentes formations. Cliquer sur la formation que vous souhaitez, pour rejoindre les différentes leçons.</p>
            </div> 
            <div class="user-content">
                    <h2>Liste des formations</h2>
                    <div class="desktop-list">
                    {% for category in categories %}
                    {%  include 'category/item.html.twig' with { categories } %}
                    {% endfor %}
                    </div>
            </div>
        </div>    
        {% endblock %}

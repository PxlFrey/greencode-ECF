<div class="sb-formation">   
   <h2>Liste des formations disponibles</h2>
        {% for category in categories %}
            <li><a href="{{ path('category_show', {'slug': category.slug, 'id': category.id}) }}">{{ category.name }}</a=></li>
        {% endfor %}
</div>
